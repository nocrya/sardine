# Sardine 当前状态与后续路线

## 1. 当前整体情况

这个项目现在已经不是纯骨架了，而是一个**有完整主链路的类 Discord 学习项目原型**。

当前已经具备的能力：

- 用户系统
  - 注册
  - 登录
  - JWT 鉴权
  - 登录态恢复
- Server / Channel
  - 创建 Server
  - 默认生成 `general` 文本频道和 `lobby` 语音频道
  - 查看我的 Server 列表
  - 查看 Server 下频道列表
- 文本消息
  - 历史消息拉取
  - 分页加载更早消息
  - 发送消息
  - 发送中 / 失败 / 重试
- 实时能力
  - WebSocket 鉴权
  - 频道消息实时推送
  - typing
  - presence
  - server 级在线状态
- 私聊
  - 创建 / 查找私聊会话
  - 私聊消息列表
  - 私聊实时收发
  - 私聊 typing
  - 私聊已读
- 权限与成员
  - `owner / admin / member`
  - 成员列表
  - 按邮箱邀请成员
  - owner 修改成员角色
- 语音阶段基础接入
  - 语音频道请求 join token
  - 前端连接 LiveKit
  - 加入 / 离开房间
  - 成员列表
  - 麦克风开关

结论：

**现在已经可以把 Sardine 当成“基础版功能较完整”的第一版。**

它已经覆盖了一个学习项目最关键的几类能力：

- 传统 CRUD 接口
- 实时消息与状态同步
- 成员与权限
- 一条最小语音链路

## 2. 当前仍然明显缺少的部分

虽然主链路已经成型，但离“基础版功能齐全并适合长期迭代”还差几块关键补齐：

### 2.1 产品层缺口

- 没有“退出 Server”
- 没有“移除成员”
- 没有“owner 转移”
- 邀请还是“按邮箱直接加入”，不是真正的邀请码 / 邀请链接
- 没有未读数
- 没有消息编辑 / 删除
- 没有好友系统
- 没有通知系统

### 2.2 实时层缺口

- WebSocket 没有断线重连和自动重订阅
- 语音房间没有 speaking 状态高亮
- Server 工作区没有展示当前语音频道成员
- 缺少更清晰的实时错误处理

### 2.3 工程层缺口

- 测试还比较薄，主要是能编过，不是业务级测试
- 没有真正的数据库迁移版本管理
- 没有日志、监控、审计
- 没有部署文档和本地一键联调方案

## 3. 接下来建议怎么做

建议不要再继续横向扩模块了，先把**第一版基础版真正收口**。

目标不是继续“看起来更像 Discord”，而是先把下面这件事做扎实：

**让当前项目成为一个功能完整、逻辑闭环、可以稳定演示的基础版 IM + 语音社区。**

## 4. 第一版目标

第一版建议定义成：

**一个支持用户登录、Server/Channel、文本聊天、私聊、基础权限、基础语音房间的可演示版本。**

这一版不追求复杂，不追求所有高级功能，只追求：

- 功能闭环完整
- 行为合理
- 数据模型稳定
- 演示时不容易崩

## 5. 第一版必须补齐的功能

下面这些是我建议的**第一版收口清单**。

### 5.1 成员与权限收口

优先级：最高

建议补齐：

1. `退出 Server`
2. `owner 移除成员`
3. `owner 转移`
4. 邀请失败和权限失败的前端提示细化

原因：

- 你已经有角色系统了，但还没有完整成员生命周期
- 没有退出、移除、转移 owner，权限系统就不算闭环

### 5.2 邀请机制升级成真正邀请

优先级：高

当前是按邮箱直接加入，这更像后台管理，不像产品功能。

建议第一版至少做到：

- 生成邀请码或 invite link
- 用户通过 invite 加入 server
- invite 可以失效或限制次数

这样更接近真实产品，也能把“加入一个社区”的入口补齐。

### 5.3 文本消息体验收口

优先级：高

建议补齐：

1. 未读数
2. 会话 / 频道列表红点
3. 消息时间展示统一
4. 基础空状态和错误状态优化

先不要做消息编辑删除。

原因：

- 这些会显著提升“像一个真实聊天产品”的感觉
- 成本比复杂消息功能低很多

### 5.4 WebSocket 稳定性

优先级：高

建议补齐：

1. 断线重连
2. 自动重订阅频道 / 私聊
3. 恢复后重新拉 snapshot

原因：

- 这是实时系统从“能跑”到“可用”的分水岭

### 5.5 语音房间基础收口

优先级：中高

建议补齐：

1. speaking 状态展示
2. 当前语音房间成员列表更稳定展示
3. 语音加入失败时的清晰错误提示
4. 工作区和语音页之间的状态联动

第一版先不做复杂音频设置、设备选择、上下麦权限。

## 6. 第一版建议的开发顺序

建议按下面顺序推进：

1. 成员生命周期闭环
   - 退出 server
   - 移除成员
   - owner 转移
2. 邀请码 / 邀请链接
3. 未读数与红点
4. WebSocket 重连与自动重订阅
5. 语音 speaking 与错误处理
6. 基础联调文档与演示路径整理

原因：

- 前 2 项决定权限系统是否完整
- 第 3、4 项决定消息系统是否“像产品”
- 第 5 项决定语音链路是否可稳定展示
- 第 6 项决定项目是否适合继续长期演进

## 7. 第一版完成标准

如果满足下面这些，我认为就可以把当前项目标记为 `v1`：

- 用户可以注册、登录、恢复会话
- 用户可以创建 server，邀请别人加入
- 用户可以看到自己的角色
- owner 可以调整成员角色、移除成员、转移 owner
- 用户可以在 server 里进行文本聊天
- 用户可以进行私聊
- 用户可以看到未读状态
- WebSocket 断线后能自动恢复
- 用户可以加入语音频道并完成最基础通话演示

## 8. 第二版优化方向

第一版做完以后，就不建议继续只补边角了，而是进入**第二版体验和结构优化**。

第二版建议分成 4 个方向。

### 8.1 产品体验优化

目标：让项目从“学习原型”变成“更像真实产品”。

建议做：

- 消息编辑 / 删除
- 回复消息
- 表情 / reaction
- 文件上传
- 用户资料页
- 更清晰的系统提示消息

### 8.2 社交能力扩展

目标：把“社区 IM”做完整。

建议做：

- 好友系统
- 用户搜索
- 更完整的私聊入口
- 群私聊 / 小群组
- 通知中心

### 8.3 语音系统增强

目标：让语音部分从“接上了”变成“能认真玩”。

建议做：

- speaking 高亮
- 房间内成员状态同步
- 静音 / 自闭麦状态广播
- 音频设备切换
- 房间状态持久化
- 工作区直接进入 voice channel，而不是单独页面

### 8.4 工程化升级

目标：把项目做成更专业、能长期维护的学习仓库。

建议做：

- 业务测试
- e2e 联调测试
- 数据库迁移工具
- Docker Compose 本地环境
- 部署脚本
- 结构化日志
- 基础监控与错误追踪

## 9. 我对后续路线的建议

如果目标是“先做一个基础版功能齐全的版本”，建议你现在进入下面这个思路：

### 先做

- 成员生命周期闭环
- 邀请链接
- 未读数
- WebSocket 重连
- 语音 speaking

### 暂时不要做

- 好友系统
- 文件上传
- 消息编辑 / 删除
- 通知中心
- 复杂权限矩阵
- 复杂语音控制台

原因很简单：

这些高级功能都是真需求，但不适合现在继续分散注意力。

你当前最需要的是：

**把现在已有的系统补齐到“完整、稳定、能演示”的程度。**

## 10. 一句话判断

Sardine 现在已经进入了：

**第一版可以收口，而不是继续无限加模块的阶段。**

最正确的后续策略不是“继续铺更多能力点”，而是先把：

**成员管理 + 邀请 + 未读 + 重连 + 基础语音体验**

这几个关键缺口补上，做成一个真正完整的 `v1`。
